-- INSERT INTO T (Default_column, Unique_Column, Constraint_Column)
-- VALUES (1,1,1),
--         (2,2,2);

-- SELECT * FROM T;

INSERT INTO T (Default_column, Unique_Column, Constraint_Column)
SELECT 3, 3, 3;

SELECT * FROM T;
-- Might not be supported in all database providers